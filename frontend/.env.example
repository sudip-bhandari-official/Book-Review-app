# Frontend environment variables (Vite only exposes VITE_* vars to the browser)
#
# For local dev, copy this to .env and (optionally) tweak VITE_API_URL.
# For Vercel, set VITE_API_URL in Project Settings → Environment Variables.

VITE_API_URL=http://localhost:5000
